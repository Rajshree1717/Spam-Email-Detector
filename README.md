# Spam-Email-Detector
This project detects whether an email is spam or not spam using machine learning algorithms like Naive Bayes, Logistic Regression, and SVM. It analyzes email text through TF-IDF vectorization to classify messages based on their content with high accuracy.
